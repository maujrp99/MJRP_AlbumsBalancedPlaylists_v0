// REMOVED: This file was intentionally cleared. Use `./js/firebase-config.js` as the canonical
// Firebase configuration. Keeping an empty stub preserves backward-compatibility for file
// references while avoiding duplicated credentials in multiple files.

// If you want this file deleted from the repository entirely, run:
//   git rm public/config.js && git commit -m "chore: remove legacy config.js"

